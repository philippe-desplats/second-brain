{
  "mcpServers": {
    "basic-memory": {
      "command": "REPLACE_WITH_BM_PATH (run: which bm, e.g. /Users/you/.local/bin/bm)",
      "args": ["mcp", "--project", "REPLACE_WITH_PROJECT_NAME"]
    }
  }
}
